import React, { useCallback, useEffect, useState } from 'react';
import uuid from 'uuid';
import { ContainerType } from '../../constant';
import { Input, Form } from 'antd';
const { Item } = Form;

export default ({ lableWidth }) => {};
