// 设置值
export default (value, rules) => {
    // rules 可以限定权限
    return value;
};

/**
 * 数值：
 * 1. 大于
 * 2. 小于
 * 3. 等于
 * 4. 不等于
 * 4. 大于等于
 * 5. 小于等于
 */

/**
 * 正则：用于匹配字符串， 非字符串的值，会转换为字符串匹配
 *
 */

/**
 * 正则：用于匹配字符串， 非字符串的值，会转换为字符串匹配
 *
 */
